<h1>New Comment</h1>

<%= form_with model: Comment.new, url: user_post_comments_path, method: :post, local: true, scope: :comment do |form| %>
  <div>
    <%= form.label :text %><br>
    <%= form.text_area :text %><br/>
    <%= form.submit "Add Comment" %>
  </div>
<% end %>
