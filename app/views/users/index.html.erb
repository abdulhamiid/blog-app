<h1>All Users</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <img src="<%= user.photo %>" alt="profile_img" width="50" height="50">
      <h2><%= link_to user.name, user %></h2>
      <p>Number of posts: <%= user.posts_counter || 0 %></p>
    </li>
  <% end %>
</ul>
